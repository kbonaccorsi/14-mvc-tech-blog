


{{!-- add in html for what this will look like --}}

*login (when needed)- active link: redirects to login endpoint
    {{#if}} html to display {{/if}}
    *button a href: sign up
    *display login form
        *username
        *password
        *button a href: submit
            *check password against hashed password
            *redirect to homepage